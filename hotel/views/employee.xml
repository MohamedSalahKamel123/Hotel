<openerp>
    <data>
        <record id="employee_hotel" model="ir.actions.act_window">
            <field name="name">Employee</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">employee_hotel</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
              <p class="oe_view_nocontent_create">
                <!-- Add Text Here -->
              </p><p>
                <!-- More details about what a user can do with this object will be OK -->
              </p>
            </field>
        </record>
        <!-- This Menu Item will appear in the Upper bar, That's why It needs NO parent or action -->
        <menuitem id="odoo_emp_hot" name="Emploee" sequence="9"/>
        <menuitem id="get_emplo" name="Emp" parent="odoo_emp_hot" action="employee_hotel" sequence="6"/>
    </data>
</openerp>